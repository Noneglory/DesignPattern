simplefactory 简单工厂模式
simple  简单工厂
multi   多个方法
staticfactory 静态工程
该模式适用于具有共同接口的类，可以通过工厂方法模式进行创建其实例。这样的工厂模式有缺陷的，那就是想要扩展程序时，必须对工
厂类进行修改，这违背了闭包原则，所以就用到了抽象工厂模式，

factorymethod 工厂方法
抽象工厂在扩展时很方便，只需要一个类来实现Sender接口，另一个类来实现Provider接口即可，不用修改原有代码

abstractfactory 抽象工厂
优点
管理多个品牌、多个类型时(多个产品，多维度)，很方便。
缺点
增加新的产品类型时比较麻烦

factorymethod是对simplefactory的扩展，优化点：不再需要修改源码
abstracfactory是对factorymethod的扩展，优化点：增加一个接口产品

设计模式没有谁比谁好，只有谁比谁更适合业务

适合场景：有大量的产品需要创建，并且他们具有共同的接口。侧重于于拿到对象使用对象的方法




